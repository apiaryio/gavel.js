{
    "description": "Any validation failures are shown in the right-hand Messages pane.",
    "type": "object",
    "required": ["foo"],
    "properties": {
        "foo": {
            "type": "any"
        },
        "bar": {
            "type": "string",
            "enum": [
                "a",
                "b",
                "c"
            ]
        }
    }
}